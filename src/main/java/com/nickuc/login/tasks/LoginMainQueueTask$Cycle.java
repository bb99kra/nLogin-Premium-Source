package com.nickuc.login.tasks;

public class LoginMainQueueTask$Cycle {
   private static int b = 4 >>> 194 | 4 << ~194 + 1;
   private static int c = (1073741824 >>> 60 | 1073741824 << -60) & -1;
   private static int f = 0 >>> 78 | 0 << ~78 + 1;
   private static int a = Integer.reverse(-1073741824);
   private static int e = (536870912 >>> 221 | 536870912 << -221) & -1;
   private int aR = a;
   private static int d = Integer.reverse(0);

   public boolean aR() {
      this.aR = this.aR + b;
      if (this.aR == c) {
         this.aR = d;
         return (boolean)e;
      } else {
         return (boolean)f;
      }
   }
}
